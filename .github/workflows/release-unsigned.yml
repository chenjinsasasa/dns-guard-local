name: Unsigned Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          package-manager-cache: false
      - name: Install yq
        run: brew install yq
      - name: Test
        run: npm test
      - name: Package unsigned app
        run: npm run package:mac
      - name: Publish release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/*-unsigned.zip
            dist/SHA256SUMS
          body: |
            ## DNS 守卫

            此构建采用 ad-hoc 签名，未经过 Apple notarization。

            - 安装前请核对 `SHA256SUMS`
            - 基础运行需要 macOS 13+ 与 Node.js 22+
            - Clash Verge Rev、TUN 与 yq 仅用于完整防护；其他客户端可使用仅检测模式
            - 首次打开可能需要在“系统设置 → 隐私与安全性”中选择“仍要打开”
          prerelease: true
